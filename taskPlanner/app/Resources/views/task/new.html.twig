{% extends 'base.html.twig' %}

{% block body %}
    <div  class="col-md-5">
        <h1>Task creation</h1>
        <div>
            {{ form_start(form) }}
            {{ form_widget(form) }}
                <input type="submit" value="Create Task" />
            {{ form_end(form) }}
        </div>
        <a href="{{ path('task_index') }}">Back to the Task list</a>

    </div>
{% endblock %}