{% extends 'base.html.twig' %}

{% block body %}
    <h1>Category creation</h1>
    <div  class="col-md-4">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}


        <a href="{{ path('category_index') }}">Back to the Category list</a>

    </div>
{% endblock %}